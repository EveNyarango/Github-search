import { HoveringDirective } from './hovering.directive';

describe('HoveringDirective', () => {
  it('should create an instance', () => {
    const directive = new HoveringDirective();
    expect(directive).toBeTruthy();
  });
});
